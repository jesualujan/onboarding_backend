{
  "name": "onboarding_backend",
  "version": "1.0.0",
  "main": "index.js",
  "author": "jesualujan <lujan.oficial@gmail.com>",
  "license": "MIT",
  "scripts": {
    "dev": "nodemon index.js",
    "migrations": "yarn prisma migrate reset --force && yarn prisma migrate dev && yarn prisma generate && yarn run seed",
    "seed": "node prisma/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^6.14.0",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "nodemon": "^3.1.10"
  },
  "devDependencies": {
    "prisma": "^6.14.0"
  }
}
